generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("OA_PG_URL")
}

model Member {
  created_at  DateTime
  email       String?  @unique
  member_id   Int      @id
  modified_at DateTime
  name        String
}

model members {
  created_at  DateTime?
  member_id   Int       @id
  modified_at DateTime?
  name        String
}